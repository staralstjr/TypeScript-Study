{
  "compilerOptions": {
    "target": "esnext",
    "module": "esnext",
    "outDir": "dist",
    "strict": false,
    // true하면 엄청 엄격하게 타입 검사!
    "moduleDetection": "force",
    // 타입스크립트는 전역 시스템으로, 다른 파일에서 동일한 변수명을 사용하지 못하게 한다.
    // 따라서 force를 주면 파일을 독립된 모듈로 인식하게 만든다. (export {}라는 코드를 추가시킨다.)
    "skipLibCheck": true
  },
  "include": ["src"]
}
